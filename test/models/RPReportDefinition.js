var assert = require('chai').assert;
var fs = require("fs");
var path = require("path");

describe('RPReportDefinition', function () {


    it('should be there', function () {

        assert.isDefined(RPReportDefinition, ' --> RPReportDefinition should be defined!');

    });

});