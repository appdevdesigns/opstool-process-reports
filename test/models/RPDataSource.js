var assert = require('chai').assert;
var fs = require("fs");
var path = require("path");

describe('RPDataSource', function () {


    it('should be there', function () {

        assert.isDefined(RPDataSource, ' --> RPDataSource should be defined!');

    });

});